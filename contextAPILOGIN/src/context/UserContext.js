import React from "react";

const UserContext=React.createContext();
//context is just like a global variable we can have access by anywhere


export default UserContext;

//now we have to add provider
